/** Base URL for the stub API server. Change this to point at a real backend. */
export const API_BASE_URL = 'http://localhost:3001'

/** Number of rows fetched per page. */
export const PAGE_SIZE = 100

/** Maximum total rows to accumulate before stopping infinite scroll. */
export const MAX_ROWS = 1000
